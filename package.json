{
  "name": "parabank-signup-automation",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "cucumber-js --require steps/**/*.js --format progress --publish-quiet features/**/*.feature",
    "install:playwright": "npx playwright install chromium"
  },
  "dependencies": {
    "@cucumber/cucumber": "^9.1.1",
    "playwright": "^1.42.0",
    "xlsx": "^0.18.5"
  }
}
